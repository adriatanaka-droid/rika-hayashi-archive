{
  "name": "rika-hayashi-archive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && HOME=.wrangler-home wrangler dev",
    "deploy": "node scripts/build.mjs && HOME=.wrangler-home wrangler deploy",
    "deploy:dry-run": "node scripts/build.mjs && HOME=.wrangler-home wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.48.0"
  }
}
